function App() {
  return (
    <main style={{ fontFamily: 'sans-serif', textAlign: 'center', marginTop: '4rem' }}>
      <h1>${{ values.name }}</h1>
      <p>React + Vite + TypeScript</p>
    </main>
  );
}

export default App;
